[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
[project]
name = "tmux-overview"
version = "0.1.0"
description = "Persistent project overview and explicit agent conversation recovery for tmux"
requires-python = ">=3.13"
dependencies = []
[project.scripts]
overview = "overview.cli:main"
[tool.setuptools]
packages = ["overview"]
