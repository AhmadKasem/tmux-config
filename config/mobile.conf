# Per-client compact status below 80 columns; keeps the desktop status intact.
# Must run after the theme and optional overview status integration.
run-shell "$HOME/.local/bin/tmux-mobile-status configure"
